# Development and CI dependency set: everything a container runs, plus the
# tooling that tests and lints it.
#
# Container images install requirements.txt only. Nothing below this line
# should ever reach a production image.

-r requirements.txt

# --- tooling / test ---
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
respx>=0.21
freezegun>=1.5
ruff>=0.6
mypy>=1.11

# No `types-redis`. redis ships inline types (`py.typed`) from v5, and the stub
# package stopped at the 4.6 API — where the async client had `close()` and not
# `aclose()`. mypy prefers a stub package over inline types, so installing it
# does not add type safety, it replaces accurate types with three-major-versions
# stale ones and fails the type check on correct code.
