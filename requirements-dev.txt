# Development and CI dependency set: everything a container runs, plus the
# tooling that tests and lints it.
#
# Container images install requirements.txt only. Nothing below this line
# should ever reach a production image.

-r requirements.txt

# --- tooling / test ---
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
respx>=0.21
freezegun>=1.5
ruff>=0.6
mypy>=1.11
types-redis
